<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :content, label: false, placeholder: "Great restaurant!" %>
  <%= f.input :rating %>
  <%= f.button :submit, value: "Envoyer !", class:"btn btn-success" %>
<% end %>
